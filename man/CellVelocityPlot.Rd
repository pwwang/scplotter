% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/celldimplot.R
\name{CellVelocityPlot}
\alias{CellVelocityPlot}
\title{Cell Velocity Plot}
\usage{
CellVelocityPlot(
  object,
  reduction,
  v_reduction,
  spat_unit = NULL,
  feat_type = NULL,
  group_by = NULL,
  ident = NULL,
  ...
)
}
\arguments{
\item{object}{A Seurat object, a Giotto object, a path to an \code{.h5ad} file,
or an opened \code{H5File} from the \pkg{hdf5r} package.}

\item{reduction}{Name of the main dimension reduction that defines cell
positions in the plot (e.g., \code{"umap"}, \code{"pca"}, \code{"tsne"}). This
reduction must already exist in the object. Unlike \code{\link{CellDimPlot}},
this parameter is required — there is no default.}

\item{v_reduction}{Name of the velocity reduction that encodes RNA velocity
vectors (e.g., \code{"stochastic_UMAP"}, \code{"dynamical_PCA"}). Only the first
two dimensions are used. This reduction must already exist in the object
and is typically generated by RNA velocity analysis tools such as
\pkg{velocyto.R} or \pkg{scVelo} (Python, exported to h5ad).}

\item{spat_unit}{Spatial unit name for Giotto objects (e.g., \code{"cell"}).
Ignored for Seurat and h5ad inputs. If \code{NULL}, the default spatial unit
is auto-detected via \code{GiottoClass::set_default_spat_unit()}.}

\item{feat_type}{Feature type name for Giotto objects (e.g., \code{"rna"},
\code{"dna"}, \code{"protein"}). Ignored for Seurat and h5ad inputs. If \code{NULL}, the
default feature type is auto-detected via
\code{GiottoClass::set_default_feat_type()}.}

\item{group_by}{Metadata column name used to color cells by group (e.g.,
\code{"CellType"}, \code{"Phase"}). Unlike \code{\link{CellDimPlot}}, this parameter is
optional — when \code{NULL}, all cells are plotted in a uniform color, which
can be useful for focusing on the velocity flow patterns without visual
distraction from cluster colors.}

\item{ident}{Alias for \code{group_by}, provided for compatibility with Seurat's
naming convention. When both \code{group_by} and \code{ident} are specified, they
must be identical.}

\item{...}{Additional arguments passed to
\code{\link[plotthis:VelocityPlot]{plotthis::VelocityPlot()}}. Key
parameters include:
\itemize{
\item \strong{Plot type:} \code{plot_type} — \code{"arrow"} (default), \code{"grid"},
or \code{"stream"} (see the \emph{Velocity plot types} section)
\item \strong{Appearance:} \code{pt_size}, \code{pt_alpha}, \code{arrow_length},
\code{arrow_thickness}, \code{palette}, \code{palcolor}, \code{theme}, \code{theme_args},
\code{legend.position}
\item \strong{Grid parameters (for \code{plot_type = "grid"}):} \code{grid_n},
\code{grid_color}, \code{grid_alpha}, \code{grid_lwd}
\item \strong{Stream parameters (for \code{plot_type = "stream"}):}
\code{stream_n}, \code{stream_color}, \code{stream_alpha}, \code{stream_lwd}
\item \strong{Layout:} \code{split_by}, \code{combine}, \code{nrow}, \code{ncol}
}}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes RNA velocity on a reduced-dimension embedding. RNA velocity
infers the future transcriptional state of individual cells by modeling the
ratio of unspliced (nascent) to spliced (mature) mRNA transcripts. On a
dimension reduction plot, velocity is displayed as arrows (or grid/stream
fields) showing the predicted direction and magnitude of transcriptional
change for each cell — effectively revealing the "flow" of cells through
differentiation, development, or other state transitions.

\code{CellVelocityPlot} serves as a unified interface across multiple single-cell
data containers:
\itemize{
\item \strong{Seurat objects} — Extracts embeddings from both the main
reduction (\code{reduction}) and the velocity reduction
(\code{v_reduction}) via \code{Embeddings()}; metadata for grouping via
\verb{@meta.data}.
\item \strong{Giotto objects} — Extracts dimension reductions via
\code{getDimReduction()} using \code{spat_unit} and \code{feat_type} to identify
the correct spatial unit and feature type.
\item \strong{h5ad files} (.h5ad or opened \code{H5File}) — Reads from \code{obsm} for
both the main and velocity embeddings; \code{obs} for metadata. Reduction
names are automatically prefixed with \code{"X_"} when needed.
}
}
\details{
See:
\itemize{
\item \url{https://pwwang.github.io/scplotter/articles/Working_with_anndata_h5ad_files.html}
}

for examples of using this function with .h5ad files.
}
\note{
\strong{Velocity computation prerequisites:} This function only \emph{visualizes}
pre-computed RNA velocity. Velocity must be calculated separately using
tools such as \pkg{velocyto.R} (R) or \pkg{scVelo} (Python). The computed
velocity reduction must already exist in the object before calling
\code{CellVelocityPlot}.

\strong{Velocity overlay in CellDimPlot:} For a quick velocity overlay on
a standard dimension reduction plot (with all \code{DimPlot} features like
highlighting, density contours, and group marks), use
\code{CellDimPlot(velocity = "stochastic_UMAP")} instead. \code{CellVelocityPlot}
is best when you need grid or stream visualizations specifically.

\strong{Required parameters:} Both \code{reduction} and \code{v_reduction} are required
and have no defaults — unlike \code{CellDimPlot()} where \code{reduction} can be
auto-detected for Seurat objects. This is because velocity visualization
intrinsically requires two distinct reductions, and auto-detection of the
velocity reduction is not reliable across analysis pipelines.
}
\section{RNA velocity background}{

RNA velocity requires two dimension reductions stored in the object:
\enumerate{
\item \strong{Main reduction} (\code{reduction}): A standard dimension reduction
(e.g., UMAP, PCA, t-SNE) computed on the cells' current expression
profiles. This defines the positions of cells in the plot.
\item \strong{Velocity reduction} (\code{v_reduction}): A dimension reduction
computed on velocity vectors derived from spliced/unspliced RNA
ratios. The first two dimensions of this reduction encode the
direction and magnitude of predicted transcriptional change.
}
Velocity reductions typically follow the naming convention
\code{"<model>_<reduction>"}, where \verb{<model>} is one of:
\itemize{
\item \code{"stochastic"} — Stochastic model of transcriptional dynamics
\item \code{"deterministic"} — Deterministic model assuming constant
transcription rates
\item \code{"dynamical"} — Dynamical model accounting for time-dependent
transcription rates
}
For example, \code{"stochastic_UMAP"} or \code{"dynamical_PCA"}.
}

\section{Relationship to CellDimPlot}{

While \code{\link{CellDimPlot}} can overlay velocity arrows on a dimension reduction
plot via its \code{velocity} parameter, \code{CellVelocityPlot} is the dedicated
velocity visualization function. The key differences are:
\itemize{
\item \code{CellVelocityPlot} delegates to
\code{\link[plotthis:VelocityPlot]{plotthis::VelocityPlot()}},
which supports multiple plot types: \code{"arrow"} (default), \code{"grid"},
and \code{"stream"}.
\item \code{CellVelocityPlot} requires both \code{reduction} and \code{v_reduction} as
explicit arguments (they have no defaults).
\item \code{CellVelocityPlot} does not support the full set of \code{DimPlot}
overlays (marks, density, lineages, etc.) — it focuses solely on
velocity visualization.
}
}

\section{Velocity plot types}{

Passed via \code{...} to \pkg{plotthis}'s \code{VelocityPlot()}, the \code{plot_type}
parameter controls how velocities are rendered:
\itemize{
\item \strong{\code{"arrow"}} (default) — An arrow is drawn from each cell's
current position to its predicted future position. Best for small
to medium datasets where individual cell trajectories are of
interest.
\item \strong{\code{"grid"}} — The embedding space is divided into a grid, and
average velocity vectors are computed per grid cell. Reduces
visual clutter for large datasets and shows regional trends.
\item \strong{\code{"stream"}} — Streamlines are computed from the velocity
field, showing continuous flow trajectories. Best for visualizing
differentiation paths and developmental trajectories.
}
}

\examples{
\donttest{
data(pancreas_sub)

# Arrow plot (default) — each cell shows its predicted direction
CellVelocityPlot(pancreas_sub, reduction = "PCA", v_reduction = "stochastic_PCA")

# Grid plot — velocity vectors averaged over grid cells
CellVelocityPlot(pancreas_sub, reduction = "PCA", v_reduction = "stochastic_PCA",
  plot_type = "grid")

# Stream plot — continuous flow trajectories
CellVelocityPlot(pancreas_sub, reduction = "PCA", v_reduction = "stochastic_PCA",
  plot_type = "stream")

# With group coloring
CellVelocityPlot(pancreas_sub, reduction = "PCA", v_reduction = "stochastic_PCA",
  group_by = "SubCellType")
}
}
\seealso{
\itemize{
\item \code{\link[plotthis:VelocityPlot]{plotthis::VelocityPlot()}} —
The underlying plotting engine supporting arrow, grid, and stream
plot types
\item \code{\link{CellDimPlot}} — Standard dimension reduction plot; can overlay
velocity arrows via the \code{velocity} parameter
\item \code{\link{CellStatPlot}} — Statistical summaries and comparisons
}
}
