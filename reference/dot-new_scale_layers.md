# Add new scale layers

Add new scale layers

## Usage

``` r
.new_scale_layers(scales)
```

## Arguments

- scales:

  The scales have been already applied in the previous layers (so that
  we need to add new scales only for the new layers).

## Value

A list of ggnewscale layers for the specified scales.
